Open
* Style management - state on the client (TradeTicketPopup) vs state on the server.
* Possible DataStar bug in data-indicator?
* Had to monkey patch fetch to auto-include header.
* Clearing signals on popup hide - expected signals to auto-clear when their defining element is removed - not the case.
* Missing JavaScript API - would be useful when doing the dragging, e.g. DataStar.setSignal('dx', x);
* Update channel needs retry: always, otherwise doesn't work
* Including all signals when fetching feels wierd, every get gets all of them serialized
* Had to add nonce to input signals to force signal reset on reRender

Solved
* Code reuse - everything is on the server (e.g. validation)
* Live reload - both server restart and hot reload work.
* Editor support - language injections
* XSS protection
* Encapsulation
    - scripts - IIFE + document.currentScript
    - styles - CSS scopes

TODO
* Securities screen
* Boost links
* commands / ordering
* extract routes
* optimize Positions
* remove loadning from trade repo - move to security repo